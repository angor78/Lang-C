#include <stdio.h>

    main(){
        
        while(getchar() != EOF){
            if (getchar() == '\t'){
                putchar() = "\t";
            }
            if (getchar() == '\b'){
                putchar() = "\b";
            }
            if (getchar() == '\'){
                putchar() = "\\";
            }
        }
                    
}
