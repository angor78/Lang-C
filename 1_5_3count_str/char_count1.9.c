#include <stdio.h>

    main(){
        int spc = 0;
        
        while(getchar() != EOF){
            if (getchar() == " "){
                ++spc;
                if( spc > 1 ){
                    putchar() = " ";
                }
            }
        }
                    
}
